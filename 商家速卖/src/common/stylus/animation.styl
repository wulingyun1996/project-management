.animation_slide_to_left
  animation slideToLeft .5s
.animation_slide_to_right
  animation slideToRight .5s

/* 
  侧滑动画：从右侧往左侧滑动进入，需要将元素设置定位，尽量为 fixed，适用于整屏侧滑  
*/
@keyframes slideToLeft
  0%
    left 100%
    right 100%
    opacity 0
  100%
    left 0
    right 0
    opacity 1

/* 
  侧滑动画：从左侧往右侧滑动进入
*/
@keyframes slideToRight
  0%
    left 0
    right 0
    opacity 1
  100%
    left 100%
    right 100%
    opacity 0